CREATE OR REPLACE TABLE URLANALYTICSREQUEST (
  url VARCHAR,
  paramsToSearch ARRAY
);

CREATE OR REPLACE VIEW URLANALYTICSREQUEST_FLATTENED AS
SELECT
  url,
  f.value as paramsToSearch
FROM URLANALYTICSREQUEST
CROSS JOIN LATERAL FLATTEN(input => paramsToSearch) f;
