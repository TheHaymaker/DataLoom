CREATE OR REPLACE TABLE URLANALYTICSRESPONSE (
  foundParams VARIANT
);

CREATE OR REPLACE VIEW URLANALYTICSRESPONSE_FLATTENED AS
SELECT
  f.value::string as foundParams
FROM URLANALYTICSRESPONSE
CROSS JOIN LATERAL FLATTEN(input => foundParams) f;
